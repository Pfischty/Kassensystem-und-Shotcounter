Die Anwendung ist nach `Shotcounter/` umgezogen. Templates, Static-Dateien und die SQLite-Datenbank liegen ausschließlich dort.

Schnellstart
------------
- Flask-CLI: `flask --app Shotcounter.app --debug run`
- Direkt: `python app.py` (leitet in `Shotcounter/app.py` weiter)

Details zur App (Routen, Hinweise) stehen in `Shotcounter/Readme.txt`.
